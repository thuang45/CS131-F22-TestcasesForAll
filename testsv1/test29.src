#test greater than or equal to, on <= and >= for strings
func main
    assign A == "A" "a"
    assign B == "#" "#"
    assign C >= "#" "#"
    assign D <= "#" "#"

    funccall print A
    funccall print B
    funccall print C
    funccall print D
endfunc